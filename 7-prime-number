public Boolean isPrime(Integer num) {
    //code here

    for ( Integer index = 3; index <= num/2; index++ ) {

        if ( math.mod(num, index) == 0 ) {

            return False;

        }

    }

    return True;

}